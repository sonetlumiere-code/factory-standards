# Roadmap

Forward-looking only. The spec describes the system *as it is*; this is what's *not done
yet*. Every item = **sketch + status + trigger**. When an item ships, collapse it to a
one-line pointer at the doc that now owns the rationale (`Documented in [spec#anchor]. Code
in [path].`) — don't grow a "what landed" changelog here; git is the record.

## Open work — carry to launch

### <Feature / gap title>

**Status:** Deferred (or: In progress / Blocked).
**Trigger:** <the concrete signal that should start this — "first operator complaint",
"first non-monotributo store", "real sandbox credentials". Not "do this next".>

**Sketch:**

- <Bullet the intended approach: schema change, the single swap point, the edge cases.>
- <Cite the real-world pattern you'd mirror (Shopify / Stripe / TiendaNube / Linear).>

**Open decisions:** <the choices to make when you pick it up.>

## Recently shipped (prune after the next release)

- <One line per shipped item, pointing at the doc + code. Delete once stale.>
